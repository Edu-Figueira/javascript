//Vamos jogar uma função dentro de uma variável. Dá para fazer isso porque o JS é uma linguagem funcional:

let v = function(x) {
    return x*2
} //leia-se: se eu receber um número (x), me retorne com o dobro dele (x*2).

console.log(v(6))

